# ML Training / Classification — Henry + Aaron

Responsible for:
- Model architecture decision
- Training and evaluation
- Improving model accuracy

## Model Checkpoints
Save trained models to `ml/models/` with version tags (e.g., `emotion_model_v1.0.pt`).
These are git-ignored due to file size — share via Google Drive or similar.

## Dataset
Javaya prepares the FER2013 data in `data/train/`, `data/val/`, `data/test/`.
Contact Javaya for the expected format or to request changes.